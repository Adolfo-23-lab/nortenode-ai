# NorteNode — Handoff 2026-04-22

## Session summary
Sesión que cubrió B.3.5 → B.4.3d.1. 13 commits pusheados a origin/main.
Se detuvo antes del sweep GSAP (B.4.3d.2) para evitar romper refactor
por auto-compact.

## Último commit en main

```
fbdb8ec docs(skills): add GSAP official skills from greensock/gsap-skills (MIT)
```

## Commits de esta tanda

```
fbdb8ec docs(skills): add GSAP official skills from greensock/gsap-skills (MIT)
ac4705e docs(contactos): post-B.4.3c baseline screenshots
9d947c7 feat(contactos): cinematic authority rework; editorial hero + typographic directory + unboxed form; kill Maps iframe; migrate Framer->GSAP
371336b feat(i18n): add error_by_code granular lookup to contactos.form; ContactForm consumes from dictionary
bcbc507 refactor(contactos): rename submitLeadAction shape (phone/sector/message) + tagged error codes
47ba495 fix(db): nullable params on submit_agency_lead + regen types (utf8) + introduce types.custom aliases
fd968e3 chore(dx): track B.4.3a-dx verification; dev:lan script; dotenv dep; scripts/test-*.ts gitignore
5ad9739 feat(db): add p_sector to submit_agency_lead + cleanup agency org contacts
444b7d0 chore(demo): kill FloatingSalesBot
7077cad feat(i18n): add demo/contactos/chat subtrees in 3 locales
08a5645 docs(system): extend §7 opacity-split rule to ring-/divide-/outline- utilities
aac9c6c docs(system): write down invariants (opacity-split, perf budget, media/motion policy, multi-tenant frontend)
7addc37 docs: solucoes post-B.3 screenshots
f912d60 feat(motion): reveals + parallax across solucoes pages
5155683 feat(solucoes/widget-web): integration + capabilities + final CTA
```

## Working tree al momento del handoff

```
 M docs/design-refs/AUDIT.md
?? docs/design-refs/ios-safari-test-protocol.md
```

Flags del working tree:
- `docs/design-refs/AUDIT.md` dirty con 4 entradas nuevas (risk Lenis
  B.3.6, asset debt logo B.4.2, metadata suffix duplication B.4.3c.3,
  locale mismatch B.4.3c.3). Se commitea junto con el resto del
  paquete B.3.6 cuando llegue el test iOS.
- `docs/design-refs/ios-safari-test-protocol.md` untracked. Se commitea
  junto con el comentario de validación en LenisProvider.tsx cuando
  el test iOS pase.

## Pendientes en orden recomendado

### B.4.3d.2 — GSAP sweep refactor (PRÓXIMO BLOQUE)
Scope definido post-auditoría del repo greensock/gsap-skills (ya
copiado a `.claude/skills/gsap/`). Tres fixes canónicos:

1. Hoist `gsap.registerPlugin(ScrollTrigger, useGSAP)` a module-level
   en `LenisProvider.tsx` (fuera del useEffect, después de imports).
   Skill: `gsap-react.md:31` + `react.instructions.md:9`.

2. Eliminar las llamadas redundantes de `gsap.registerPlugin(ScrollTrigger)`
   dentro de useGSAP callbacks en todos los componentes. Verificar
   lista exacta con grep, pero son ~11-15 archivos bajo
   `src/components/` y `src/components/solucoes/` y `src/components/contactos/`.
   Skill: `gsap-scrolltrigger.md:274` + `scrolltrigger.instructions.md:9`.

3. Añadir `document.fonts.ready.then(() => ScrollTrigger.refresh())`
   en LenisProvider post-init de Lenis.
   Razón: Instrument Serif con `display: "swap"` causa layout shift
   tras font swap; triggers calculados pre-swap quedan desalineados.
   Skill: `gsap-scrolltrigger.md:261` + `275` + `284` + `scrolltrigger.instructions.md:16`.

Commit sugerido:
`refactor(motion): hoist GSAP plugin registration to module-level;
refresh ScrollTrigger post document.fonts.ready`

### B.4.3d.3 — Fix FAQ gsap.context()
`src/components/FAQ.tsx:116-147` — `useEffect([open])` con `gsap.to(...)`
sin `gsap.context()` ni cleanup. Riesgo de leak on unmount mid-animation.
Skill cita exactamente este caso: `gsap-react.md:65` ("when the effect's
dependency/trigger behavior is needed").

Fix: envolver en `gsap.context(() => {...}, panelRef)` +
`return () => ctx.revert()`.

Commit sugerido:
`fix(faq): wrap accordion tween in gsap.context() for safe cleanup`

### B.4.3c.4 — Polish visual /contactos (BLOQUEADO por decisión del owner)
Owner reportó que el hero visual no le convence. Screenshots en
`docs/design-refs/post-b4-3c/`. Referencias mencionadas: Stripe +
Vercel (sans-serif dominante, cero italic, serif solo como acento).

Diagnóstico pendiente: Instrument Serif podría no estar cargando
correctamente (la clase `font-[var(--font-display)]` puede no resolver
a la variable CSS correctamente en Tailwind v4).

Owner está eligiendo entre:
  (A) Sans dominante + serif solo como acento — ¿sí o no?
  (B) Hero vacío editorial vs añadir metadata card.
  (C) Botón CTA estilo Stripe (pill signal-blue) vs Vercel (borderless glass).

Sin respuestas del owner, el polish no arranca.

### B.4.4 — /demo rework (BLOQUEADO por /contactos polish)
Scope: kill ConversionZone, hero editorial, DemoStage (InteractiveDemo
full-stage con ghost hairline vertical separando narrative del chat),
narrative 3-step. Requiere extender `t.demo` con `how_steps[]` + `cta_*`.

Depende de: (a) polish /contactos cerrado primero (replicar patrón
convalidado), (b) decisión sobre si extender `t.demo` o reutilizar
`t.solucoes.common.cta`.

### B.3.6 — Validar Lenis en iOS Safari (BLOQUEADO por device access del owner)
Owner debe correr test en iPhone real siguiendo protocolo en
`docs/design-refs/ios-safari-test-protocol.md`. Dev server:
`npm run dev:lan` → `http://192.168.1.2:3000`.

Cuando test pase:
- Añadir comentario de validación en `src/components/motion/LenisProvider.tsx`.
- Commit unificado con: AUDIT.md + package.json + .gitignore +
  ios-safari-test-protocol.md + LenisProvider comment.

Si test falla, diagnosticar y fix específico.

### Deuda tracked sin fecha (ver AUDIT.md §4.e)
- Meta WhatsApp sandbox (añadir +351937809995 al allowlist o production review).
- Migration drift: 3 migrations remote sin local (pre-B.7).
- Metadata suffix duplication en `<title>` (afecta todas las rutas).
- Locale mismatch metadata (pt hardcoded) vs UI (es via I18nProvider).
- PNG logo 60KB → SVG inline (baja prioridad).

## Invariantes escritos — NO mutar sin migración explícita
- `AGENTS.md`: Performance budget, Media policy, Motion policy, Copy
  rules / agency positioning, Multi-tenant frontend rules.
- `docs/design-refs/DESIGN.md` §7: Opacity-Split Rule (aplica a
  `text-/border-/bg-/ring-/divide-/outline-/shadow-/fill-/stroke-/
  from-/via-/to-*white/NN`).
- `.claude/skills/design-system-cinematic-authority.md`: stance visual.
- `.claude/skills/gsap/*`: patrones canónicos GreenSock oficiales (MIT).

## Commands útiles vistos en esta tanda
- `supabase db query --linked "..."`
- `supabase db query --linked --file <path>`
- `supabase migration repair --status applied <version> --linked`
- `supabase gen types typescript --linked --schema public`
- PowerShell .NET `WriteAllText` con `UTF8Encoding($false)` para escribir
  types sin BOM (gotcha Windows — `iconv` y `Out-File -Encoding utf8NoBOM`
  no funcionaron en PS 5.1).
- `npx tsx scripts/test-*.ts` (scripts gitignored bajo `scripts/test-*.ts`)
- `npm run dev:lan` (expone next dev en 0.0.0.0 para test iOS)

## Próxima acción al retomar en sesión nueva
1. Leer este archivo completo.
2. Verificar que `git log --oneline -15` coincide con la sección
   "Commits de esta tanda".
3. El owner trabaja con un segundo Claude en claude.ai como planning
   layer. Él traerá el brief de B.4.3d.2 con las citas literales de
   las skills embebidas (ya hizo la extracción en la sesión anterior).
4. Arrancar con B.4.3d.2 cuando el brief llegue.
